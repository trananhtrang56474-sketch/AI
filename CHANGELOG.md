# Changelog

本项目记录 **AI-Counselor（心理健康咨询系统）** 的全部重要迭代历程，涵盖算法、系统架构、多模态交互与伦理安全设计。
# 📅 2026-02-02 开发进度记录

## 🧱 核心架构升级 (Architecture)
- **Pinia 全局状态管理重构**
  - 🧠 引入 `Pinia` 作为全局状态管理方案，创建 `src/stores/chatStore.js`。
  - 🔄 将原本分散在 `ChatPage.vue` 内的 `conversation`（聊天记录）与 `chartData`（情绪图表数据）统一迁移至 Store。
  - 🚀 利用 Pinia 单例特性，实现会话级数据的**内存缓存**，解决 `/chat ↔ /home` 切换导致的聊天内容丢失与白屏问题。
  - ⚙️ 在 `main.js` 中完成 `createPinia()` 全局注册，修复因 Store 未挂载导致的应用启动异常。

- **会话级数据模型确立**
  - 📦 采用 `{ sessionId: messages[] }` 的结构存储多会话聊天数据。
  - 支持多 Session 并存、快速切换，为后续「会话列表 / 搜索 / 归档」打下基础。

## 🧭 路由与组件缓存优化 (Routing & Lifecycle)
- **KeepAlive 组件缓存**
  - ♻️ 在 `App.vue` 中对 `ChatPage` 启用 `<KeepAlive>`，避免路由切换时组件被销毁。
  - 配合显式 `name: 'ChatPage'`，确保缓存策略精准生效。

- **路由监听逻辑重写**
  - 🔍 重构 `watch(route.query.session_id)` 逻辑，区分：
    - 页面切换（不清空数据）
    - 会话切换（按需加载或命中缓存）
  - 🧯 修复因错误判断导致的「返回聊天页内容消失」问题。

- **生命周期体验修复**
  - ⏫ 使用 `onActivated` 钩子，在组件从缓存唤醒时自动滚动至最新消息。
  - 🧹 在 `onDeactivated / onUnmounted` 中清理定时器，避免后台打字机逻辑持续运行。

## ✨ 新增功能 (Features)
### 💬 会话管理能力
- **删除历史会话**
  - 🗑️ 前端：侧边栏新增删除按钮（Hover 显示），并配合自定义确认弹窗。
  - 🧩 后端：新增 `DELETE /api/sessions/{id}` 接口，支持级联删除 `ChatSession` 与 `ChatLog`。
  - ✅ 删除后自动同步更新前端 Store 状态，避免脏数据残留。
# 📅 2026-01-30 开发进度记录
## ✨ 核心体验升级 (Core Experience)
- **全局轻提示系统 (Global Toast)**
  - ✨ 创建了 `GlobalToast.vue` 组件与 `toast.js` 状态管理。
  - 🚀 移除了原生丑陋的 `alert` 弹窗，替换为悬浮的磨砂玻璃胶囊提示。
  - ✅ 在登录/注册流程中集成了 Toast 反馈（欢迎提示、错误报错）。
- **页面丝滑转场 (Page Transitions)**
  - 🔄 在 `App.vue` 中添加了全局 `page-fade` 动画。
  - 实现了页面切换时“云雾散开”般的淡入淡出与微缩放效果，消除了白屏顿挫感。

## 💬 聊天窗口优化 (Chat Window)
- **图片消息视觉重构**
  - 🎨 移除了图片消息外层的气泡背景与内边距 (`image-bubble`)。
  - 实现图片在聊天流中的独立悬浮显示，视觉更清透。
- **图片预览模态框 (Lightbox)**
  - 🔍 优化了预览体验：限制最大宽高 (900px / 85vh)，防止大屏下图片过大。
  - 🌫️ 背景升级为清透的“磨砂玻璃”效果 (`backdrop-filter: blur`)，取代了压抑的深黑色背景。
  - ✨ 添加了弹窗入场动画 (`pop-in`) 和缩放手势提示。
- **拟人化打字效果**
  - 🤖 将 AI 加载时的“跳动圆点”升级为带有闪烁光标 (`▋`) 的“正在思考...”动画，增强生命力。
## ⌨️ 输入区域增强 (Input Area)
- **输入框悬浮布局**
  - 🛠️ 修复了底部 `glass-footer` 的布局问题，实现了输入框真正的悬浮感，增加了底部呼吸空间。
- **图片上传预览**
  - 🖼️ 新增输入框上方的图片缩略图预览功能。
  - 支持一键移除待发送图片，并实现了灵动的弹出动画。
## 🐛 其他修复
- 修复了 `App.vue` 中组件引入路径别名 (`@/`) 可能导致的 Vite 解析错误。
- 统一了全站的 Z-Index 层级，确保 Toast 和模态框始终位于最顶层。
# 📅 2026-01-29 开发进度记录
## 🎨 视觉与体验重构 (UI/UX Overhaul)
- **全站玻璃拟态系统 (Full Glassmorphism System)**:
  - 彻底重构 UI 架构，移除所有不透明白色背景，实现**全透明玻璃 + 动态呼吸背景** (`App.vue` 全局接管)。
  - 引入 `MainLayout` 透明化方案，解决子组件背景遮挡问题。
- **动态呼吸背景 (Dynamic Breathing Background)**:
  - 新增全局 CSS 动画 `gradientBG`，实现 "暖阳 -> 天蓝 -> 柔粉" 的 15s 循环呼吸效果。
- **组件视觉升级 (Component Upgrades)**:
  - **Sidebar**: 重构为悬浮玻璃侧边栏，选中状态支持**主色调光晕**与动态高亮。
  - **Home**: 首页卡片全面玻璃化，图标颜色语义化（焦虑橙/平静绿），移除冗余背景层。
  - **ChatWindow**: 聊天气泡升级，用户侧采用**主色渐变** (`Linear Gradient`)，AI 侧采用高透磨砂白。
  - **AsidePanel**: 右侧面板实现 "实时 Live" 徽标呼吸效果，心理图表卡片增加微光质感。

## 🛠️ 前端架构 (Frontend Architecture)
- **主题变量化 (CSS Variables System)**:
  - 在 `:root` 中定义中央调色板，统一管理 `--primary-color` (主色)、`--glass-bg` (玻璃浓度)。
  - 新增 `--primary-rgb` 变量，支持使用 `rgba(var(--primary-rgb), 0.1)` 动态计算同色系透明度。
  - 实现**"一键换肤"**底层架构，修改一个变量即可全站变色。
- **路由与布局优化 (Routing & Layout)**:
  - 移除了 `router.js` 中过时的嵌套逻辑，确立了 `App.vue` -> `MainLayout` -> `Views` 的透明层级关系。
  - 优化了 `LoginPage.vue` 的入场动画 (`floatUp`) 与模式切换体验。

## 🐛 修复 (Fixes)
- 修复了 `MainLayout` 背景色导致全局渐变被遮挡的问题 (`background: transparent !important`)。
- 修复了 `AsidePanel` 中情绪颜色与主题色不匹配的硬编码问题。
- 解决了 `ChatPage` 在宽屏下背景层级 (`z-index`) 导致的点击穿透问题。
# 📅 2026-01-28 开发进度记录
## 🚀 重点功能 (Major Features)
- **企业级邮箱认证系统 (Enterprise Email Auth)**:
  - 重构前端认证组件 `LoginPage.vue`，支持 **“登录/注册”双模式** 无缝切换。
  - 实现基于 **SMTP** 的验证码发送流程，集成 `60s` 倒计时与邮箱格式正则校验。
  - 新增后端 `/api/send-code` 接口，支持验证码的生成、内存存储 (`verification_codes`) 与校验闭环。
- **UI 视觉与交互升级 (Visual & UX Upgrade)**:
  - 引入 **Glassmorphism (磨砂玻璃)** 设计语言，采用商务蓝紫渐变 (`#667eea` -> `#764ba2`)。
  - 增加动态背景动画 (`Floating Orbs`) 与输入框聚焦/报错震动反馈 (`Shake Animation`)。
  - 优化路由体验，支持通过 `/register` 路径自动重定向唤起注册模式。

## ⚙️ 后端与架构 (Backend & Architecture)
- **邮件服务集成 (Flask-Mail Integration)**:
  - 在 `extensions.py` 与 `app.py` 完成 `Flask-Mail` 的标准化配置与初始化。
  - 预置 **163 / 126 / QQ** 三套主流邮箱服务商配置，支持一键切换。
  - 修复中文发件人昵称的 `ascii` 编码错误 (采用 `formataddr` 标准格式)。
- **演示兜底机制 (Simulation Mode)**:
  - 针对无外网或 SMTP 端口被封锁的演示环境，新增 **“模拟发送”** 逻辑。
  - 当邮件发送失败时，自动降级为 **后端控制台打印验证码**，确保毕设答辩流程 **100% 可用**。
## 🐛 修复 (Fixes)
- 修复了 `router/index.js` 中因缺失 `MainLayout` 组件导致的 Vite 编译报错。
- 修复了注册接口未校验验证码导致的安全漏洞。
---
## [2.2.0] - 2026-01-23
## 🚀 重点功能 (Major Features)
- **量化心理学计算 (Quantified Self)**: 
  - 引入 **Russell 情绪环状模型**，将情绪标签映射到三维向量空间（`价向 Valence`、`唤醒度 Arousal`、`风险 Risk`）。
  - 构建动态 **心理健康指数 (0-100 分)**：
    - `0-40`：负面/高危区间（折线呈现暖色警告）。
    - `41-70`：平静/中性区间（折线呈现治愈冷色）。
    - `71-100`：积极/心流区间（折线呈现明亮活力色）。
- **实时心理轨迹仪表盘 (ECharts Visualization)**:
  - 在聊天页面顶部新增可折叠的 **情绪监测面板**。
  - 封装 `EmotionChart.vue` 组件，支持贝塞尔平滑曲线与基于分数分段的动态染色。
  - 实现后端数据库、前端状态机与可视化图表的毫秒级同步刷新。

## 🧠 算法与 AI (Algorithm & AI)
- **“假性平静”探测算法 (Pseudo-Calmness Detection)**: 
  - 新增针对“情感压抑”模式的识别逻辑（识别高唤醒负面转低唤醒平静的断崖式变化）。
  - 联动 `GENTLE_PROBING`（温和面质）策略，使 AI 能识破用户掩饰的情绪，提供更深层的关怀。
- **数据持久化升级**:
  - 完成数据库迁移，在 `ChatLog` 表中新增 `emotion_score` 字段，支持心理数据的长效追踪。
  - 优化 `/api/chart-data` 接口，支持历史数据的自动加权估算。

## 🐛 修复与优化 (Fixes)
- 修复了 ECharts 在容器高度受限时底部 X 轴标签被截断的问题。
- 优化了顶部面板的折叠动画，提升了聊天窗口的可用空间。
---
# 📅 2026-01-13 开发进度记录

本次更新致力于让 AI 从“冷冰冰的问答机器”进化为**“具备临床思维与共情能力的数字咨询师”**，实现了心理状态的实时监测与可视化。

## 🧠 算法与策略层 (Algorithm & Policy)

- **9 分类情绪图谱构建**
  - **分类体系扩展**：从原有的 6 类临床标签扩展至 **9 类全维情感**。
    - **新增**：`积极 (POSITIVE)`、`迷茫 (CONFUSION)`、`悲伤 (GRIEF)`、`愧疚 (GUILT)`。
    - **意义**：不仅能识别病理状态，更能捕捉用户的闪光点（积极心理学）与成长困惑（迷茫/自责）。
  - **Prompt 深度优化**：重写 `analyze_emotion` 提示词，显著提升了对非典型负面情绪（如“我很内疚”）的识别精度。

- **心理测评防御门控 (Assessment Gate - Hard Interception)**
  - **机制升级**：从“提示词引导”升级为 **“RAG 硬拦截”**。
  - **逻辑**：当 RAG 检索到 PHQ-9/GAD-7 等量表时，若用户当前情绪非负面（如“平静”或“积极”），系统强制丢弃量表内容。
  - **价值**：彻底解决了“用户只是开心闲聊，AI 却硬要用户测抑郁”的体验缺陷，保护咨访关系。

- **积极心理学知识注入**
  - 引入马丁·塞利格曼的 **PERMA 模型** 与 **心流理论 (Flow)**。
  - 新增 `STRENGTH_BUILDING`（优势探索）策略分支，当识别到“积极”情绪时，AI 会主动运用“品味 (Savoring)”技术进行正向强化。

- **状态回溯机制 (State Restoration)**
  - 升级 `/api/history` 接口，返回结构从单纯的 `messages` 升级为 `{ messages, analysis }`。
  - 实现历史会话的 **“心境还原”**：点击历史记录，不仅恢复文字，更能还原当时的“情绪标签”与“AI 策略”。

## 💻 前端交互与可视化 (Frontend & Visualization)

- **实时心理映像仪表盘 (Real-time Mind Monitor)**
  - 在侧边栏新增 **LIVE 分析模块**，无缝融入原有 UI。
  - **Vue Store 状态驱动**：利用 `store.js` 实现全局状态管理，聊天回复到达毫秒级内，侧边栏自动刷新。
  - **动态可视化**：
    - **情绪标签**：根据情绪类型自动变色（如：焦虑=橙色，平静=绿色，危机=红色闪烁）。
    - **策略透视**：实时展示 AI 正在使用的咨询技术（如“🧠 认知重构”、“❤️ 深度共情”），增加系统透明度与专业感。

- **工程稳定性**
  - 修复前端 `import` 命名冲突导致的白屏问题。
  - 解决 Windows 环境下 Flask 重载导致的 `WinError 10038` 端口占用问题。

---

# 📅 2026-01-10 开发进度记录

本次更新标志着系统从简单的关键词匹配转向基于语义的深度检索，大幅提升了对用户模糊表达的理解能力。

## 🧠 核心架构升级 (RAG 2.0)

- **向量数据库集成 (Vector Database Integration)**
  - **ChromaDB 部署**：正式引入向量数据库作为知识存储核心。
  - **语义嵌入**：集成 `sentence-transformers (all-MiniLM-L6-v2)` 模型，将文本转化为高维语义向量。
  - **语义检索**：实现基于余弦相似度的检索机制，解决“词汇不同但意图相同”的召回难题。

- **智能知识构建管线 (ETL Pipeline)**
  - 重构 `build_knowledge.py` 为递归式文档扫描引擎。
  - **非结构化处理**：自动对 `.txt` 理论书籍进行切片（Chunking）与向量化。
  - **结构化处理**：针对 PHQ-9 等心理量表（.json）进行特殊解析。
  - **元数据标签**：根据目录结构自动注入 `category: cbt`, `stage: crisis` 等标签，支持混合检索。

## 📚 数据层重构 (Data Layer)

建立符合学术标准的目录结构 (`backend/data/`)：
- **DSM-5 诊断标准**：收录抑郁/焦虑核心症状。
- **CBT 干预体系**：存储认知扭曲识别指南与 E-SCBA 理论框架。
- **伦理安全机制**：基于《中国心理学会伦理守则》确立危机熔断机制。
- **标准化量表**：存储结构化测评数据。

---

# 📅 2026-01-09 开发进度记录

重点在于打通“视觉+文本”的双通道交互，并将心理学理论转化为可执行的工程逻辑。

## 🔬 理论与算法落地

- **E-SCBA 框架工程化**：实现“情感约束”与“主题约束”的双重控制生成机制。
- **7 周 CBT 干预范式**：内置从情绪命名到行为激活的完整干预流程。
- **高危信号识别**：增强对“图文不符”及“微笑抑郁”等隐性消极情绪的计算能力。

## 📸 多模态视觉交互

- **双模型智能切换引擎**
  - **纯文本流**：路由至 `qwen-plus`。
  - **图文混合流**：路由至 **Qwen-VL-Max**，实现视觉与语义的联合理解。
- **全栈图片处理链路**
  - **前端**：支持相机调用、Blob 本地预览、Lightbox 全屏查看。
  - **后端**：新增 `/api/upload` 静态资源服务，支持 Base64 流式处理。

---

# 📅 2026-01-02 开发进度记录

## 🎨 界面与交互重构

- **全局 Sidebar 组件化**：统一管理历史记录、折叠动画与响应式布局。
- **路由驱动机制**：利用 Vue Router 的 `query` 参数管理会话状态。
- **EventBus 通信**：实现跨组件无感同步刷新。

---

# 📅 2025-12-25 开发进度记录

项目从 0 到 1 的突破，完成了基础设施搭建与核心链路跑通。

## 🎉 核心功能上线

- **基础架构**：Flask + Vue 3 前后端分离。
- **AI 核心接入**：接入阿里云通义千问 (Qwen-Plus) API。
- **数据持久化**：User / ChatSession / ChatLog 核心表设计 (SQLAlchemy)。
- **原型验证**：实现基础用户系统与关键词检索功能。